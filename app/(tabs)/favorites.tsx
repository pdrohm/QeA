import React from 'react';
import { Box, Text } from '../../src/theme/components';

export default function TabFavoritesScreen() {
  return (
    <Box flex={1} backgroundColor="mainBackground" padding="m">
      <Text variant="header">Favorites</Text>
    </Box>
  );
} 